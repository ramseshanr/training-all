apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-example
  namespace: ns01
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: manual